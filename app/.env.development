# Vite supports dotenv-expand.
# Mobile development can't use localhost so we use a private address instead.
VITE_API_URL="http://${TAURI_DEV_HOST:-localhost}:3000"
VITE_APP_URL="http://${TAURI_DEV_HOST:-localhost}:1420"
