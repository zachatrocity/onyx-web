#!/usr/bin/env just --justfile

# Using Just: https://github.com/casey/just?tab=readme-ov-file#installation

# List all of the available commands.
default:
  just --list

# Run the CI checks
check:
	cargo check
	cargo clippy -- -D warnings
	cargo fmt -- --check

# Automatically fix some issues
fix:
	cargo fmt
	cargo clippy --fix --allow-dirty --allow-staged

# Build the native app for the current platform
build:
	bun tauri build --no-bundle

# Deploy the desktop app for the target platform
# Load secrets from 1password, signs for the target platform, and uploads the result.
deploy target:
	#!/usr/bin/env bash
	VERSION=$(cargo pkgid | cut -d'@' -f2)

	# Build but don't sign the app yet, just to minimize secrets exposure
	bun tauri build --no-bundle --target "{{target}}"

	# Sign the app using secrets from 1password
	op run --env-file=".env" -- ./bin/tauri-bundle --target "{{target}}"

	DMG=$(basename target/{{target}}/release/bundle/dmg/hang_${VERSION}_*.dmg)
	SIGNATURE=$(cat "target/{{target}}/release/bundle/macos/hang.app.tar.gz.sig")

	# Upload the artifacts to R2
	echo "Uploading artifacts to R2..."
	bun wrangler r2 object put "download-hang-live/{{target}}/$VERSION/hang.tar.gz" --remote --file "target/{{target}}/release/bundle/macos/hang.app.tar.gz"
	bun wrangler r2 object put "download-hang-live/{{target}}/$VERSION/hang.tar.gz.sig" --remote --file "target/{{target}}/release/bundle/macos/hang.app.tar.gz.sig"
	bun wrangler r2 object put "download-hang-live/{{target}}/$VERSION/hang.dmg" --remote --file "target/{{target}}/release/bundle/dmg/$DMG"
	echo '{"version":"'$VERSION'","platforms":{"{{target}}":{"signature":"'$SIGNATURE'","url":"https://download.hang.live/{{target}}/'$VERSION'/hang.tar.gz"}}}' | bun wrangler r2 object put "download-hang-live/{{target}}/latest.json" --remote --pipe

# Build, sign, and upload all the platform variants for the Mac app
deploy-mac:
	just deploy "x86_64-apple-darwin"
	just deploy "aarch64-apple-darwin"
	#just deploy "universal-apple-darwin"

# Run the native app in development mode
dev:
	bun tauri dev

android flags="":
	bun tauri android dev {{flags}}

ios:
	bun tauri ios dev

# Generate Tauri icons from browser-generated ZIP files
# Prerequisites: Download all ZIP files from http://localhost:1420/dev/icons to this directory
icons:
	@echo "Generating Tauri icons from ZIP files..."
	@rm -rf icons

	# Extract desktop icons directly to final location
	@echo "Extracting desktop icons..."
	@mkdir -p icons
	cd icons && unzip -q ../icons-default.zip

	# Generate Windows ICO file
	@echo "Generating Windows ICO..."
	cd icons && magick 16x16.png 24x24.png 32x32.png 48x48.png 64x64.png 256x256.png 512x512.png icon.ico
	@mv icons/512x512.png icons/icon.png

	# Extract macOS icons directly to final location
	@mkdir -p icons/macos
	@echo "Extracting macOS icons..."
	cd icons/macos && unzip -q ../../icons-macos.zip
	@echo "Generating macOS ICNS with png2icns..."; \
	cd icons/macos && png2icns icon.icns 16x16.png 32x32.png 64x64.png 128x128.png 256x256.png 512x512.png 1024x1024.png; \

	# Extract and organize Android icons to gen/android folders
	@echo "Extracting Android icons..."
	@unzip -o -q icons-android-launcher.zip -d gen/android/app/src/main/res
	@unzip -o -q icons-android-foreground.zip -d gen/android/app/src/main/res
	@unzip -o -q icons-android-background.zip -d gen/android/app/src/main/res
	@unzip -o -q icons-android-round.zip -d gen/android/app/src/main/res

	# Extract iOS icons to gen/apple folders
	@echo "Extracting iOS icons..."
	@unzip -o -q icons-ios.zip -d gen/apple/Assets.xcassets/AppIcon.appiconset

	@echo "✅ Icon generation complete!"
